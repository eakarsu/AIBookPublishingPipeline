const test=require('node:test');const assert=require('node:assert/strict');const{createClient,normalizeList}=require('../public/api');
test('normalizes raw and paginated manuscript collections',()=>{assert.deepEqual(normalizeList([{id:1}]),[{id:1}]);assert.deepEqual(normalizeList({data:[{id:2}]}),[{id:2}]);});
test('publishing client authenticates manuscript creation',async()=>{let captured;const client=createClient({baseUrl:'http://api.test',getToken:()=> 'editor-token',fetchImpl:async(url,options)=>{captured={url,options};return{ok:true,status:201,json:async()=>({id:3,title:'Proof'})};}});const result=await client('/manuscripts',{method:'POST',body:JSON.stringify({title:'Proof'})});assert.equal(result.id,3);assert.equal(captured.url,'http://api.test/manuscripts');assert.equal(captured.options.headers.Authorization,'Bearer editor-token');assert.equal(captured.options.headers['Content-Type'],'application/json');});
